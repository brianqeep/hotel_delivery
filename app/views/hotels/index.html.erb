<% if user_signed_in?%>
<%= link_to "Register hotel", new_hotel_path%>
<ul>
  <% @hotels.each do |hotel|%>
    <li><%= link_to hotel.name, hotel_path(hotel)%></li>
    <%= hotel.user.email%>
  <%end%>

</ul>
<%end%>
